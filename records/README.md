# 棋谱资料

`famous-games.js` 是应用「棋谱」目录使用的名局索引（119 局），SGF 已内联，运行时无需额外文件。`famous/` 保存部分对应的 SGF 源文件。

来源为 CWI / A. E. Brouwer 公有领域棋谱档案，每条记录在 `sourceUrl` 标注来源；仅保留对局信息与实战主线，不含解说文字。

在应用「打开」中可浏览内置名局；也可选择任意包含 `.sgf` 的文件夹批量导入（浏览器会在你授权后读取，导入记录仅保留到关闭或刷新页面为止）。
